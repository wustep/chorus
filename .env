# TODO: server/client terms are a little ambiguous right now, revise later

# SERVER is the data generator file with commands and additional server settings


PORT=3000

# CLIENT is the public-facing directory to post via Express
CLIENT=public/

# DEBUG determines whether or not to show all the server-side commands. By default, only errors are shown
DEBUG=true